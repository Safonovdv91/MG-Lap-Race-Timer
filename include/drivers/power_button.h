#pragma once

#include <Arduino.h>

// Драйвер кнопки включения/выключения.
// Логика:
//   - короткое нажатие ничего не делает (можно расширить при желании)
//   - удержание кнопки HOLD_TIME_MS (см. .cpp) -> уход в deep sleep
//   - повторное нажатие этой же кнопки -> аппаратное пробуждение (ext0),
//     после которого ESP32 заново проходит setup()

// Вызывается один раз в setup(), до использования остальной периферии,
// которая может быть завязана на тот же пин/состояние.
void powerButton_init();

// Вызывается каждую итерацию loop().
void powerButton_loop();

typedef void (*PowerButtonSleepHook)();
